/**
 * Comprehensive phrase dictionary and dynamic translation helper for Russian localization.
 * Used for dynamic fallback translation and DOM text normalization.
 */
export declare const PHRASE_MAP_RU: Record<string, string>;
/**
 * Translates a single text phrase, checking exact dictionary match first, then pattern rules.
 * Returns the translated Russian string if matched, or undefined if unchanged.
 */
export declare function translatePhrase(text: string): string | undefined;
//# sourceMappingURL=phrases.d.ts.map