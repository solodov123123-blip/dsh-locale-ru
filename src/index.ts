/** Host registration for the Russian locale plugin. */
import type { Context } from '@deepseek-ai/cordis'

export const name = 'dsh-client-locale-ru'

/**
 * Host-side registration of the Russian localization plugin.
 * @param _ctx - Host context
 */
export function apply(_ctx: Context): void {
  // Plugin declaration on host
}
